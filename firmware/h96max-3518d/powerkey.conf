# Nothing can wake this board: no RTC, no IR, and BLE wake is unfixed. Suspend would trap it and
# poweroff is one-way, so both actions are ignored until there is a safe target.
[Login]
HandlePowerKey=ignore
HandlePowerKeyLongPress=ignore
