--- board.dts
+++ armbian.dts
@@ -1,11 +1,13 @@
 /dts-v1/;
 
 / {
-	compatible = "rockchip,rk3518-evb1-ddr4-v10", "rockchip,rk3518";
+	/* board name first: the Seekwave driver builds its firmware filenames from it.
+	 * rk3528a last, because rkmpp has no rk3518 entry and cannot otherwise find the VPU */
+	compatible = "h96max-3518d,rk3518-tvbox", "rockchip,rk3518-evb1-ddr4-v10", "rockchip,rk3518", "rockchip,rk3528a";
 	interrupt-parent = <&gic>;
 	#address-cells = <0x02>;
 	#size-cells = <0x02>;
-	model = "Rockchip RK3518 EVB1 DDR4 V10 Board";
+	model = "H96 Max 3518_DG_ZX_V01";
 
 	aliases {
 		ethernet0 = "/ethernet@ffbd0000";
@@ -496,6 +498,7 @@
 			reg = <0x00 0xfe500000 0x00 0x400000>;
 			interrupts = <0x00 0x98 0x04>;
 			dr_mode = "host";
+			/* NOT FITTED - USB 3, but left wired: removing usb3-phy breaks xhci suspend (-110) */
 			phys = <&u2phy_otg &combphy_pu 0x04>;
 			phy-names = "usb2-phy", "usb3-phy";
 			phy_type = "utmi_wide";
@@ -767,6 +770,9 @@
 			mode-charge = <0x5242c30b>;
 			mode-fastboot = <0x5242c309>;
 			mode-loader = <0x5242c301>;
+			/* `reboot maskrom` from the OS: the SPL reads this register on the way
+			 * up and hands to the BootROM, so no button and no serial are needed */
+			mode-maskrom = <0xef08a53c>;
 			mode-normal = <0x5242c300>;
 			mode-recovery = <0x5242c303>;
 			mode-ums = <0x5242c30c>;
@@ -845,9 +851,10 @@
 		compatible = "arm,mali-450";
 		reg = <0x00 0xff700000 0x00 0x40000>;
 		interrupts = <0x00 0x58 0x04 0x00 0x59 0x04 0x00 0x56 0x04 0x00 0x5c 0x04 0x00 0x5d 0x04 0x00 0x5a 0x04 0x00 0x5b 0x04>;
-		interrupt-names = "Mali_GP_IRQ", "Mali_GP_MMU_IRQ", "IRQPP", "Mali_PP0_IRQ", "Mali_PP0_MMU_IRQ", "Mali_PP1_IRQ", "Mali_PP1_MMU_IRQ";
-		clocks = <&scmi_clk 0x16 &cru 0x113 &cru 0x10e>;
-		clock-names = "clk_mali", "aclk_gpu_mali", "pclk_gpu";
+		/* names mainline lima expects; the vendor Mali blob used its own */
+		interrupt-names = "gp", "gpmmu", "pp", "pp0", "ppmmu0", "pp1", "ppmmu1";
+		clocks = <&cru 0x113 &cru 0x113>;
+		clock-names = "bus", "core";
 		assigned-clocks = <&scmi_clk 0x16>;
 		assigned-clock-rates = <0x11e1a300>;
 		power-domains = <&power 0x04>;
@@ -1080,7 +1087,8 @@
 		iommus = <&vop_mmu>;
 		rockchip,grf = <&grf>;
 		status = "okay";
-		esmart_lb_mode = [03];
+		/* factory 03 caps every Esmart window at 2K; 02 gives Esmart0 a 4K line buffer */
+		esmart_lb_mode = [02];
 		support-multi-area;
 		phandle = <0xea>;
 
@@ -1263,7 +1271,8 @@
 		rockchip,grf = <&grf>;
 		nvmem-cells = <&vdac_out_current &test_version>;
 		nvmem-cell-names = "out-current", "version";
-		status = "okay";
+		/* NOT FITTED - composite video encoder, and this board has no AV jack */
+		status = "disabled";
 		phandle = <0xef>;
 
 		ports {
@@ -1665,7 +1674,9 @@
 		pinctrl-0 = <&pwm3m0_pins>;
 		clocks = <&cru 0x7f &cru 0x7e>;
 		clock-names = "pwm", "pclk";
-		status = "okay";
+		/* NOT FITTED - no IR receiver. The bundled remote drives the board over IR, but nothing
+		 * here answers it. Left enabled the node only costs a shared group-IRQ storm. */
+		status = "disabled";
 		remote_pwm_id = <0x03>;
 		handle_cpu_id = <0x01>;
 		remote_support_psci = <0x00>;
@@ -1784,7 +1795,8 @@
 		clocks = <&cru 0x73 &cru 0x72>;
 		clock-names = "tclk", "pclk";
 		interrupts = <0x00 0x1c 0x04>;
-		status = "disabled";
+		/* /dev/watchdog for systemd RuntimeWatchdogSec - also the only hang recorder */
+		status = "okay";
 		phandle = <0x10d>;
 	};
 
@@ -1915,7 +1927,8 @@
 		#sound-dai-cells = <0x00>;
 		pinctrl-names = "default";
 		pinctrl-0 = <&spdifm0_pins>;
-		status = "okay";
+		/* NOT FITTED - no SPDIF connector on this board */
+		status = "disabled";
 		phandle = <0x9b>;
 	};
 
@@ -1939,7 +1952,9 @@
 		phy-handle = <&rmii0_phy>;
 		nvmem-cells = <&macphy_bgs>;
 		nvmem-cell-names = "bgs";
-		status = "okay";
+		/* NOT FITTED - no ethernet PHY. Probing costs a boot delay and two errors for nothing
+		 * (RK630 PHY stmmac-0:02: phy_poll_reset failed: -110) */
+		status = "disabled";
 		phandle = <0x10e>;
 
 		mdio0: mdio {
@@ -2016,7 +2031,8 @@
 		assigned-clock-rates = <0x5f5e100>;
 		#address-cells = <0x01>;
 		#size-cells = <0x00>;
-		status = "okay";
+		/* NOT FITTED - SFC with no flash: SPL reports "unrecognized JEDEC id bytes: 00, 00, 00" */
+		status = "disabled";
 		phandle = <0x111>;
 	};
 
@@ -2073,7 +2089,8 @@
 		resets = <&cru 0x2a9>;
 		reset-names = "reset";
 		rockchip,use-v2-tuning;
-		status = "okay";
+		/* NOT FITTED - no SD slot; the controller binds and claims vcc-sd/vccio_sd regardless */
+		status = "disabled";
 		bus-width = <0x04>;
 		cap-mmc-highspeed;
 		cap-sd-highspeed;
@@ -2259,6 +2276,7 @@
 		reset-names = "combphy-apb", "combphy";
 		rockchip,pipe-grf = <&grf>;
 		rockchip,pipe-phy-grf = <&grf>;
+		/* unused here, but must stay enabled: disabling it breaks xhci suspend */
 		status = "okay";
 		phandle = <0x23>;
 	};
@@ -2318,7 +2336,8 @@
 		clock-names = "pclk", "mclk";
 		resets = <&cru 0x1ad>;
 		reset-names = "acodec";
-		status = "okay";
+		/* NOT FITTED - no analog audio output on this board */
+		status = "disabled";
 		pa-ctl-gpios = <&gpio0 0x00 0x00>;
 		phandle = <0x91>;
 	};
@@ -3797,7 +3816,8 @@
 	};
 
 	acodec_sound: acodec-sound {
-		status = "okay";
+		/* NOT FITTED - sound card for the absent analog codec */
+		status = "disabled";
 		compatible = "simple-audio-card";
 		simple-audio-card,name = "rk3528-acodec";
 		simple-audio-card,format = "i2s";
@@ -3914,7 +3934,8 @@
 	};
 
 	spdif-sound {
-		status = "okay";
+		/* NOT FITTED - sound card for the absent SPDIF */
+		status = "disabled";
 		compatible = "simple-audio-card";
 		simple-audio-card,name = "ROCKCHIP,SPDIF";
 		simple-audio-card,mclk-fs = <0x80>;
@@ -4103,7 +4124,9 @@
 		compatible = "gpio-leds";
 		phandle = <0x1c7>;
 
-		work-green {
+		/* power/standby are the names the shared LED hooks drive; the factory called
+		 * them work-green and work-red */
+		power {
 			gpios = <&gpio4 0x11 0x01>;
 			linux,default-trigger = "none";
 			default-state = "on";
@@ -4111,7 +4134,8 @@
 			retain-state-shutdown;
 		};
 
-		work-red {
+		/* the standby half of the same rename; the factory called it work-red */
+		standby {
 			gpios = <&gpio4 0x0b 0x01>;
 			linux,default-trigger = "none";
 			default-state = "off";
@@ -4120,10 +4144,9 @@
 		};
 	};
 
-	chosen: chosen {
-		bootargs = "earlycon=uart8250,mmio32,0xff9f0000 console=ttyFIQ0 driver_async_probe=dwmmc_rockchip,rockchip-drm drm_kms_helper.fbdev_emulation=0";
-		phandle = <0x1c8>;
-	};
+	/* chosen dropped: u-boot overwrites /chosen/bootargs from its own env before handing the
+	 * tree to Linux (fdt_chosen -> board_fdt_chosen_bootargs -> env_get), so anything here is
+	 * inert. The cmdline lives in armbianEnv.txt, per board via BOARD_SERIALCON. */
 
 	fiq-debugger {
 		compatible = "rockchip,fiq-debugger";
