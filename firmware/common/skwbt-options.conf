# The factory chip images assert on the HCI codec reads (BSPASSERT:hci_tl.c-386) and never finish
# controller init; 0005 answers those opcodes locally. Every board on that chip code needs it.
options skwbt skip_codec_reads=1
