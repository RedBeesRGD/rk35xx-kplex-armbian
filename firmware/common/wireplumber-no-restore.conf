# Every boot identical: no remembered per-application sink, no remembered volumes.
# The sink is ranked by rk35xx-audio-select from the HDMI connector's state, and a
# remembered target would outrank it.
wireplumber.settings = {
  stream.restore-target = false
  stream.restore-props = false
  device.restore-props = false
}
