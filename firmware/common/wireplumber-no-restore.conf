# Every boot identical: no remembered default sink, no remembered per-application
# target, no remembered volumes. rk35xx-output-select ranks the sinks from the live
# display, and any of these would outrank it.
wireplumber.settings = {
  node.restore-default-targets = false
  node.stream.restore-target = false
  node.stream.restore-props = false
}
