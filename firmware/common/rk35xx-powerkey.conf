# No PMIC: poweroff parks the SoC with the rails up, so a short press suspends instead.
# Long press is BLE-only (IR stops repeating before systemd's 5 s); poweroff is safe, IR wakes it.
[Login]
HandlePowerKey=suspend
HandlePowerKeyLongPress=poweroff
